[project]
name = "ontotheke"
version = "0.0.0"
description = "Development workspace for the Ontotheke OWL 2 tooling family"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "zensical",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/*"]
